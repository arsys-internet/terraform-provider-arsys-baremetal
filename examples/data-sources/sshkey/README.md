# SSH key

This example demonstrates how to query SSH key information using the Arsys Baremetal provider.

## What This Example Does

- Queries all available SSH keys using `arsys-baremetal_ssh_keys`
- Gets detailed information about a specific SSH key using `arsys-baremetal_ssh_key`
- Outputs the list of SSH keys and details of a specific one