# Subnet

This example demonstrates how to create and manage subnets using the Arsys Baremetal provider.

## What This Example Does

- Creates a subnet using `arsys-baremetal_subnet`
- Output subnet information for reference
- Deletes a subnet using `terraform destroy -target=arsys-baremetal_subnet.name_of_subnet`
